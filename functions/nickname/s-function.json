{
  "name": "nickname",
  "runtime": "nodejs4.3",
  "description": "methods for working with a user's nickname",
  "customName": false,
  "customRole": false,
  "handler": "handler.handler",
  "timeout": 10,
  "memorySize": 1536,
  "authorizer": {},
  "custom": {
    "optimize": {
      "exclude": ["aws-sdk"]
    }
  },
  "endpoints": [
    {
      "path": "nickname",
      "method": "POST",
      "type": "AWS",
      "authorizationType": "AWS_IAM",
      "authorizerFunction": false,
      "apiKeyRequired": false,
      "requestParameters": {},
      "requestTemplates": "$${authenticatedTemplate}",
      "responses": {
        "400": "$${default400ResponseTemplate}",
        "default": "$${default200ResponseTemplate}"
      }
    },
    {
      "path": "nickname",
      "method": "OPTIONS",
      "type": "MOCK",
      "requestTemplates": "$${corsOptionsRequestTemplate}",
      "responses": "$${corsOptionsResponseTemplate}"
    }
  ],
  "events": [],
  "environment": {
    "USERS_TABLE": "${users-table}"
  },
  "vpc": {
    "securityGroupIds": [],
    "subnetIds": []
  }
}